version: '3.3'
services:
  wiremock:
    image: wiremock/wiremock
    ports:
      - '8080:8080'
    container_name: wiremock
    volumes:
      - '$PWD/data/wiremock/extensions:/var/wiremock/extensions'
